namespace: Kanmail v2
language: EN
working_dir: /opt/kanmail
sast:
  include:
    - .
  exclude:
    # External/minified
    - frontend/public/iframe/darkreader.min.js
    # Uses unpinned docker images (builds them, CI only)
    - build/security/scan.sh
    # Testing code, not compiled in
    - internal/tests/
sca:
  include:
    - .
