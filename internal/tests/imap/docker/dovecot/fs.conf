# Kanmail IMAP test server: Dovecot 2.3, Maildir LAYOUT=fs ("/" delimiter, no prefix)
!include common.conf.inc

mail_location = maildir:~/Maildir:LAYOUT=fs

namespace inbox {
  inbox = yes
  separator = /
  prefix =
  !include mailboxes.conf.inc
}
