# Kanmail IMAP test server: Dovecot 2.3, Maildir++ ("." delimiter, no prefix)
!include common.conf.inc

mail_location = maildir:~/Maildir

namespace inbox {
  inbox = yes
  separator = .
  prefix =
  !include mailboxes.conf.inc
}
